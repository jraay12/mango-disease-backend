services:
  - type: web
    name: mango-disease-api
    runtime: python
    runtimeVersion: "3.11.1"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host=0.0.0.0 --port=10000"
    plan: free
